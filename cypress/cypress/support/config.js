import './commands';

/**
 * Global configuration and behavior for Cypress tests.
 */

// Import custom commands

// Import plugins if needed (usually configured in cypress/plugins/index.js)
// Example: import './plugins'; // Uncomment if you have a plugins file

// You can add global before/after hooks or configuration here

// Example: Set a default timeout for all tests
Cypress.config('defaultCommandTimeout', 8000);